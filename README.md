# DTS to FLAC Converter

Сервис для автоматической конвертации аудиодорожек DTS-HD MA в FLAC в видео файлах с веб-интерфейсом управления. Оптимизирован для эффективной работы с использованием Go.

## Особенности

- 🚀 **Оптимизация ресурсов** - низкое потребление памяти (10-20MB)
- 🎬 **Автоматическое сканирование** директорий на наличие файлов с DTS аудио
- 📋 **Полуавтоматический режим** через файл `2CONVERT`
- ⏰ **Планировщик** для автоматического запуска по расписанию
- 🌐 **Веб-интерфейс** для мониторинга и управления
- 📊 **Отслеживание прогресса** в реальном времени через WebSocket
- 🔄 **Система очередей** с возможностью повтора ошибок
- 📝 **Детальное логирование** всех операций

## Быстрый старт

### Требования

- Docker & Docker Compose
- Доступ к директориям с видео файлами
- Место на диске для конвертированных файлов
- **Рекомендуется**: минимум 1GB RAM, 2GB свободного места

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd audio-converter
```

2. Настройте пути к видео файлам в `docker-compose.yml`:
```yaml
volumes:
  - /path/to/your/videos:/videos:ro
  - /path/to/output:/output
```

3. Запустите сервис:
```bash
docker-compose up -d
```

4. Откройте веб-интерфейс: http://localhost:3000

### Первоначальная настройка

1. **Настройте директории** в разделе "Configuration"
2. **Укажите путь к файлу 2CONVERT** (если используется)
3. **Настройте расписание** автоматического сканирования
4. **Запустите первое сканирование** для проверки

## Использование

### Ручной режим

1. Перейдите в раздел "Scan"
2. Укажите директорию для сканирования
3. Нажмите "Start Scan"
4. Выберите найденные файлы для конвертации
5. Нажмите "Add to Queue"

### Полуавтоматический режим

1. Создайте файл `2CONVERT` с путями к файлам:
```
/videos/movie1.mkv
/videos/movie2.mkv
/media/series/episode1.mkv
```

2. В веб-интерфейсе нажмите "Parse 2CONVERT"
3. Файлы автоматически добавятся в очередь

### Автоматический режим

1. В настройках укажите расписание в формате cron:
   - `0 */6 * * *` - каждые 6 часов
   - `0 2 * * *` - каждый день в 2:00
   - `*/30 * * * *` - каждые 30 минут

2. Включите "Auto Start"
3. Сервис будет автоматически сканировать директории и добавлять файлы в очередь

## Мониторинг

### Статус очереди

- **Pending** - файлы в очереди на обработку
- **Processing** - идет конвертация
- **Completed** - успешно конвертировано
- **Failed** - ошибка конвертации

### Просмотр логов

В разделе "Logs" можно видеть:
- Запуск и завершение задач
- Ошибки конвертации
- Системные сообщения
- Прогресс обработки

### Статистика

В разделе "Dashboard" отображается:
- Общее количество обработанных файлов
- Процент успешных конвертаций
- Среднее время обработки
- Текущая загрузка системы

## Конвертация

### Что делает сервис

Сервис конвертирует только аудиодорожки, оставляя видео и субтитры без изменений:

- **Вход**: файлы с аудиодорожками DTS-HD MA
- **Выход**: те же файлы с аудиодорожками FLAC
- **Качество**: битрейт 384k, 7.1 каналов
- **Скорость**: копирование видео без перекодирования

### Пример конвертации

Исходный файл:
```
movie.2023.2160p.DTS-HD.MA.5.1.HDR10.HEVC-GROUP.mkv
```

Результат:
```
movie.2023.2160p.FLAC-7.1.HDR10.HEVC-GROUP.mkv
```

## Технологический стек

### Backend
- **Go (Golang)** - высокопроизводительный бэкенд
- **Gin** - HTTP фреймворк
- **SQLite** - база данных
- **FFmpeg** - конвертация аудио

### Frontend
- **Vanilla JavaScript** - легкий фронтенд
- **WebSocket** - real-time обновления

### Контейнеризация
- **Docker** - изоляция зависимостей
- **Nginx** - статический сервер

## API

### Основные эндпоинты

- `GET /api/queue` - получить очередь задач
- `POST /api/queue` - добавить задачу в очередь
- `DELETE /api/queue/:id` - удалить задачу
- `GET /api/config` - получить конфигурацию
- `PUT /api/config` - обновить конфигурацию
- `GET /api/status` - статус сервиса

### WebSocket события

- `task_progress` - прогресс задачи
- `task_status` - изменение статуса задачи

## Troubleshooting

### Частые проблемы

**Файлы не находятся при сканировании**
- Проверьте права доступа к директориям
- Убедитесь что пути в конфигурации правильные
- Проверьте что файлы имеют расширения .mkv, .mp4, .avi

**Ошибки конвертации**
- Проверьте доступное место на диске
- Убедитесь что исходный файл не поврежден
- Проверьте логи в разделе "Logs"

**Медленная конвертация**
- Уменьшите количество одновременных задач
- Проверьте загрузку CPU
- Убедитесь что диск достаточно быстрый

### Логирование

Логи сохраняются в директории `./logs`:
- `app.log` - основные события приложения
- `conversion.log` - детальные логи конвертации
- `error.log` - ошибки и предупреждения

## Конфигурация

### Основные параметры

- `scanDirectories` - директории для сканирования
- `scanInterval` - интервал автоматического сканирования (cron)
- `twoConvertPath` - путь к файлу 2CONVERT
- `maxConcurrentTasks` - максимальное количество одновременных задач (рекомендуется 1)
- `autoStart` - автоматический запуск при старте

### Продвинутые настройки

- `retryCount` - количество попыток повтора при ошибке
- `logLevel` - уровень детализации логов
- `tempDir` - временная директория для конвертации

## Безопасность

### Рекомендации

- Ограничьте доступ к веб-интерфейсу через firewall
- Используйте HTTPS в продакшене
- Регулярно делайте бэкап конфигурации
- Мониторьте использование дискового пространства

### Права доступа

Сервис требует:
- Чтение из директорий с исходными файлами
- Запись в директорию с результатами
- Чтение/запись в директорию с логами

## Обновление

### Обновление до новой версии

1. Сохраните текущую конфигурацию
2. Остановите сервис: `docker-compose down`
3. Получите обновления: `git pull`
4. Пересоберите образы: `docker-compose build`
5. Запустите сервис: `docker-compose up -d`

### Производительность

Сервис оптимизирован для эффективной работы:
- Потребление памяти: 10-20MB
- Одна задача за раз для стабильности
- Быстрый запуск (0.1 сек)
- Минимальная нагрузка на CPU

### Резервное копирование

```bash
# Сохранить конфигурацию
docker-compose exec backend cp /app/data/database.sqlite ./backup/

# Сохранить логи
cp -r ./logs ./backup/logs-$(date +%Y%m%d)
```

## Поддержка

Если возникли проблемы:

1. Проверьте раздел "Logs" в веб-интерфейсе
2. Посмотрите логи контейнера: `docker-compose logs backend`
3. Проверьте системные ресурсы: `docker stats`
4. Создайте issue с подробным описанием проблемы

## Лицензия

MIT License - см. файл LICENSE